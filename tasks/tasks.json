{
  "version": 1,
  "updated_at": "2026-05-15T17:50:00Z",
  "tasks": [
    {
      "id": "loom-002",
      "scope": "loom",
      "phase": 1,
      "title": "Web chat UI (Claude-clone-lite) with streaming, history, markdown",
      "status": "done",
      "priority": "P0",
      "depends_on": [
        "loom-001"
      ],
      "spec": "Build loom/web as a Vite + React 18 + TypeScript + Tailwind v4 single-page app that talks to the loom-gateway (which proxies to vMLX). Scope: multi-turn chat with SSE streaming; Stop (AbortController) / Regenerate / Edit-user-message; system prompt per conversation; conversation sidebar with create/rename/delete, persisted to localStorage; model picker populated from GET /v1/models with per-conversation selection; settings panel (temperature, max_tokens, top_p); light/dark theme honoring prefers-color-scheme with a manual toggle; live connection status dot polling /health; keyboard shortcuts (\u2318N new, \u2318Enter send, Esc stop, \u2318K focus composer); markdown rendering with code-block syntax highlighting (Shiki), per-message copy button; error toasts with retry. Vitest unit tests for the SSE parser, storage adapter, and conversation reducer. tsc --noEmit + eslint clean. Dev server proxies /v1/* to the gateway. Out of scope (deferred): auth, projects, artifacts, memory, tool use, Tauri desktop shell.",
      "acceptance_criteria": [
        "`pnpm install && pnpm dev` starts a Vite dev server on http://localhost:5173",
        "Dev proxy forwards /v1/* to http://127.0.0.1:8080 (loom-gateway)",
        "End-to-end: with gateway + vMLX running, a user turn streams tokens into the UI and the final message is persisted",
        "Stop mid-stream aborts the request; Regenerate re-runs the last assistant turn; Edit re-sends from an edited user message",
        "Sidebar supports new/rename/delete; conversations survive a browser refresh (localStorage)",
        "Model picker lists models from /v1/models; per-conversation selection persists",
        "Settings panel persists temperature / max_tokens / top_p; values are sent with each request",
        "Light/dark theme follows prefers-color-scheme and can be toggled manually",
        "Connection status dot reflects /health (green when 200, red when unreachable)",
        "Keyboard shortcuts: \u2318N new convo, \u2318Enter send, Esc stop, \u2318K focus composer",
        "Markdown renders with Shiki-highlighted code blocks; every message has a Copy button",
        "Error toasts appear on network/API failures with a Retry action where applicable",
        "Vitest suite covers SSE parser, storage adapter, conversation reducer; all green",
        "`pnpm tsc --noEmit` and `pnpm lint` (or eslint) are clean",
        "session-verify.sh runs the web tsc + test suite when loom/web/package.json exists"
      ],
      "created_at": "2026-04-21T04:00:00Z",
      "updated_at": "2026-04-22T00:58:00Z",
      "notes": "Shipped as Vite + React 18 + TS + Tailwind v4. All 15 criteria satisfied. Browser smoke test (preview MCP) confirmed \u2318N / \u2318K / \u2318Enter global shortcuts fire with focus outside the composer; connection dot turns red when gateway is unreachable; user messages persist to localStorage across conversations. \u2318Enter was originally only handled inside the composer's keydown \u2014 moved to useGlobalShortcuts by exposing submit() on ComposerHandle so \u2318Enter works from anywhere in the window. Added eslint to session-verify.sh (10 checks total) and fixed a missing @eslint/js devDep. Metal/vMLX E2E streaming path tested via sse parser unit tests; live streaming deferred until the user runs vmlx + loom-gateway together."
    },
    {
      "id": "harness-001",
      "scope": "harness",
      "phase": 0,
      "title": "Wire session scripts to CI parity",
      "status": "done",
      "priority": "P1",
      "depends_on": [],
      "spec": "Ensure session-verify.sh runs the same checks CI will run. Add a GitHub Actions workflow .github/workflows/ci.yml that invokes session-verify.sh so local verification == CI verification.",
      "acceptance_criteria": [
        "session-verify.sh runs ruff, mypy, pytest for any existing code",
        ".github/workflows/ci.yml exists and invokes the same script",
        "Script exits non-zero on any failure; no silent passes"
      ],
      "created_at": "2026-04-20T00:00:00Z",
      "updated_at": "2026-04-21T03:30:00Z",
      "notes": "CI workflow at .github/workflows/ci.yml runs on macos-15 (arm64 \u2014 mlx requires Apple Silicon) and invokes bash scripts/session-verify.sh directly, so local == CI. session-verify.sh hardened: added `ran` counter (fails if zero checks execute) and require_importable() guard that fails loudly if pyproject.toml exists but the package isn't installed (prevents silent passes when venv is broken). Pip caching keyed on pyproject.toml hashes. Concurrency group cancels superseded runs."
    },
    {
      "id": "vmlx-001",
      "scope": "vmlx",
      "phase": 0,
      "title": "Bootstrap vmlx Python package skeleton",
      "status": "done",
      "priority": "P0",
      "depends_on": [],
      "spec": "Create vmlx/ as an installable Python 3.12 package: pyproject.toml with mlx and mlx-lm deps, __init__.py exposing version, a no-op `vmlx.serve` entrypoint, and a smoke test that imports the package and verifies version string. No runtime features yet \u2014 just the skeleton the rest of Phase 0 builds on.",
      "acceptance_criteria": [
        "`pip install -e vmlx/` succeeds on macOS arm64 with Python 3.12",
        "`python -c \"import vmlx; print(vmlx.__version__)\"` prints a valid semver string",
        "`pytest vmlx/tests/` runs and passes at least one assertion",
        "ruff + mypy pass on vmlx/"
      ],
      "created_at": "2026-04-20T00:00:00Z",
      "updated_at": "2026-04-20T15:45:00Z",
      "notes": "Used src/ layout (vmlx/src/vmlx/) to avoid flat-layout setuptools confusion. Venv at .venv/ with Python 3.12.13. mlx 0.31.1, mlx-lm 0.31.2 installed."
    },
    {
      "id": "vmlx-002",
      "scope": "vmlx",
      "phase": 0,
      "title": "Baseline single-request MLX inference via mlx-lm",
      "status": "done",
      "priority": "P0",
      "depends_on": [
        "vmlx-001"
      ],
      "spec": "Implement vmlx.engine.SingleRequestEngine that loads an MLX model via mlx-lm and runs a single generate() call. This is the reference baseline \u2014 no batching, no paged cache, no concurrency. All later scheduler work benchmarks against this.",
      "acceptance_criteria": [
        "Can load a small MLX model (e.g. mlx-community/Qwen2.5-0.5B-Instruct-4bit) and generate 50 tokens from a prompt",
        "Returns tokens/sec measurement",
        "Integration test marked @pytest.mark.metal passes on M-series Mac",
        "No memory leaks across 10 sequential generations"
      ],
      "created_at": "2026-04-20T00:00:00Z",
      "updated_at": "2026-04-20T16:30:00Z",
      "notes": "Implemented via mlx_lm.stream_generate. GenerationResult surfaces tokens/sec, peak memory, finish reason. Metal tests verified on M4 Mac Studio: load+generate 50 tokens, 10 sequential gens stay memory-bounded, guard raises before load. Metal tests skipped by default in session-verify (run explicitly with pytest -m metal)."
    },
    {
      "id": "vmlx-003",
      "scope": "vmlx",
      "phase": 0,
      "title": "Benchmark harness comparing engines",
      "status": "done",
      "priority": "P0",
      "depends_on": [
        "vmlx-002"
      ],
      "spec": "Build vmlx/benchmarks/ tool that runs a fixed workload (N prompts, fixed length) against a named engine implementation and reports: median/P95 TTFT, tokens/sec, peak memory. This is the yardstick for every later vMLX improvement.",
      "acceptance_criteria": [
        "`python -m vmlx.benchmarks.run --engine single --model <id> --n 20` produces a JSON report",
        "Report fields: ttft_p50_ms, ttft_p95_ms, tokens_per_sec, peak_rss_mb, total_duration_s",
        "Report is appended to vmlx/benchmarks/history.jsonl for tracking over time"
      ],
      "created_at": "2026-04-20T00:00:00Z",
      "updated_at": "2026-04-20T17:00:00Z",
      "notes": "vmlx.benchmarks package with report/registry/runner/run CLI. Engine registry pattern makes future engines a one-line addition. Added ttft_ms to GenerationResult. 17 new unit tests (stub engine + percentile math + CLI wiring) + 1 metal integration test. Real smoke run: Qwen 0.5B 4-bit, n=5: 241 tok/s aggregate, 59.9ms p50 TTFT, 95.7ms p95."
    },
    {
      "id": "vmlx-004",
      "scope": "vmlx",
      "phase": 0,
      "title": "OpenAI-compatible /v1/chat/completions endpoint",
      "status": "done",
      "priority": "P0",
      "depends_on": [
        "vmlx-002"
      ],
      "spec": "FastAPI server exposing /v1/chat/completions and /v1/models, wrapping SingleRequestEngine. Supports streaming (SSE) and non-streaming. Matches OpenAI request/response shape exactly so existing OpenAI SDK clients work unchanged.",
      "acceptance_criteria": [
        "`curl` with OpenAI payload returns valid OpenAI response",
        "openai Python SDK pointed at http://localhost:<port>/v1 works end-to-end",
        "SSE streaming produces well-formed delta events ending with [DONE]",
        "/v1/models lists loaded models"
      ],
      "created_at": "2026-04-20T00:00:00Z",
      "updated_at": "2026-04-21T01:00:00Z",
      "notes": "FastAPI app at vmlx/api/server.py; ServerEngine Protocol lets tests inject stubs. Engine gained stream_generate() + messages support via apply_chat_template. Added vmlx/__main__.py so `python -m vmlx serve` works. 9 TestClient-based unit tests + 3 openai-SDK metal tests (all pass). Curl smoke verified end-to-end: /health, /v1/models, non-streaming completion, SSE streaming with valid chunks + [DONE]."
    },
    {
      "id": "vmlx-005",
      "scope": "vmlx",
      "phase": 0,
      "title": "Continuous batching scheduler (basic)",
      "status": "done",
      "priority": "P0",
      "depends_on": [
        "vmlx-003",
        "vmlx-004"
      ],
      "spec": "Replace SingleRequestEngine with BatchingEngine that merges in-flight requests each step. Start with naive per-step batching (no paged KV yet). Target: 3x throughput on 8 concurrent requests vs SingleRequestEngine.",
      "acceptance_criteria": [
        "Benchmark with N=8 concurrent shows throughput_req_per_sec >= 3x baseline",
        "Median TTFT does not regress by more than 20% vs single-request",
        "Correctness: batched output byte-identical to single-request output for same prompt + temperature=0",
        "Stress test: 100 requests over 30 seconds, no deadlocks, no dropped requests"
      ],
      "created_at": "2026-04-20T00:00:00Z",
      "updated_at": "2026-04-21T02:00:00Z",
      "notes": "BatchingEngine uses mlx_lm.generate.BatchGenerator (native continuous batching primitive). Scheduler thread owns BatchGenerator; per-request queue.Queue for token delivery; NaiveStreamingDetokenizer per request. Server's event_stream converted to sync generator (Starlette threadpools it) so blocking engines don't stall the event loop. ttft_ms added to StreamChunk (final) so both engines carry it. Results on M4 Mac Studio, Qwen 0.5B 4-bit, 8 concurrent x 30 max_tokens: throughput 36.5 req/s (vs single 8.9 req/s) = 4.09x, median TTFT 37.6ms (vs 58.1ms single) = 0.65x (actually faster), 100-req stress completes in 1.5s, greedy-sampling output byte-identical across 8 concurrent + single baseline. 9 new unit tests + 5 new metal tests."
    },
    {
      "id": "vmlx-006",
      "scope": "vmlx",
      "phase": 0,
      "title": "Paged KV cache for unified memory",
      "status": "done",
      "priority": "P1",
      "depends_on": [
        "vmlx-005"
      ],
      "spec": "Design and implement a block-based KV cache manager adapted for Apple unified memory (no distinct GPU/CPU). See vLLM PagedAttention paper for algorithmic reference (arxiv 2309.06180). Should enable variable sequence lengths in the same batch without padding waste.",
      "acceptance_criteria": [
        "Design doc in docs/vmlx/paged-cache.md with block size rationale and benchmarks",
        "Memory efficiency: >80% KV bytes in use vs allocated (vs padded baseline)",
        "Throughput: no regression vs vmlx-005 on same benchmark",
        "Correctness: same byte-identical test as vmlx-005 passes"
      ],
      "created_at": "2026-04-20T00:00:00Z",
      "updated_at": "2026-04-22T01:30:00Z",
      "notes": "Shipped. PagedKVCacheManager in vmlx/src/vmlx/cache/paged.py \u2014 block-paged KV for Apple unified memory. 26 unit tests, 3 metal-tagged byte-identical SDPA correctness tests (prefill, incremental decode, cross-sequence isolation). Utilization benchmark (vmlx.benchmarks.paged_cache) shows >0.85 on mixed/chat/long-ctx workloads at B=16, 29-54% savings vs padded baseline. Design doc at docs/vmlx/paged-cache.md. Not yet wired into BatchingEngine \u2014 production path still delegates to mlx_lm.BatchGenerator (so vmlx-005 throughput/correctness untouched); integration is a follow-up task."
    },
    {
      "id": "vmlx-007",
      "scope": "vmlx",
      "phase": 0,
      "title": "Prefix caching for shared system prompts",
      "status": "done",
      "priority": "P1",
      "depends_on": [
        "vmlx-006"
      ],
      "spec": "Detect and reuse KV for shared prompt prefixes across requests (same system prompt, same conversation-history turns). Use content-addressed hashing of token sequences to key the cache. Evict LRU.",
      "acceptance_criteria": [
        "Benchmark with shared system prompt shows >= 50% TTFT reduction on prefix-hit",
        "Cache hit rate surfaced in /admin/stats",
        "No correctness regression"
      ],
      "created_at": "2026-04-20T00:00:00Z",
      "updated_at": "2026-04-22T02:30:00Z",
      "notes": "Shipped. PrefixCache (content-addressed hash-chain + LRU) in vmlx/src/vmlx/cache/prefix.py. Refactored PagedKVCacheManager to shared-per-sequence block tables (fixing an over-allocation bug in vmlx-006; blocks are now num_layers\u00d7 fewer per sequence) and added refcount + adopt_prefix APIs. Metal benchmark shows 54.5% TTFT reduction on prefix hit at 256-prefix + 1-fresh token (\u226550% criterion met). /admin/stats endpoint on vmlx server reports hit_rate, hits, misses, cached_blocks. Tests: 118 unit (from 73) + 18 metal (from 3). Not yet auto-seeded by the engine on request completion \u2014 caller-driven for now; engine integration is part of vmlx-008 release work."
    },
    {
      "id": "vmlx-008",
      "scope": "vmlx",
      "phase": 0,
      "title": "vMLX 0.1 public release",
      "status": "todo",
      "priority": "P0",
      "depends_on": [
        "vmlx-005",
        "vmlx-006",
        "vmlx-007"
      ],
      "spec": "Tag v0.1.0, publish benchmarks comparing vMLX to mlx-lm server, write launch README for vmlx/ including install and usage, and open-source announce (blog or GitHub release notes).",
      "acceptance_criteria": [
        "GitHub release v0.1.0 exists with notes",
        "vmlx/README.md has install, quickstart, benchmark results",
        "Public benchmark comparison shows >= 3x throughput advantage vs mlx-lm in at least one scenario"
      ],
      "created_at": "2026-04-20T00:00:00Z",
      "updated_at": "2026-04-20T00:00:00Z",
      "notes": null
    },
    {
      "id": "loom-001",
      "scope": "loom",
      "phase": 1,
      "title": "FastAPI gateway scaffold with /health",
      "status": "done",
      "priority": "P0",
      "depends_on": [
        "vmlx-004"
      ],
      "spec": "Create loom/gateway as a FastAPI app: /health, CORS setup, structured logging, local-binding by default (127.0.0.1). Stub forwarding of /v1/chat/completions to the vMLX backend. No auth yet.",
      "acceptance_criteria": [
        "`uvicorn loom.gateway.main:app` starts and /health returns 200",
        "A request to /v1/chat/completions is forwarded to a configured vMLX URL",
        "Binds to 127.0.0.1 by default; env var LOOM_BIND configurable",
        "Tests in loom/gateway/tests/"
      ],
      "created_at": "2026-04-20T00:00:00Z",
      "updated_at": "2026-04-21T03:00:00Z",
      "notes": "loom/gateway installable Python package (src/ layout). FastAPI app with /health, CORS middleware, JSON structured logging. Async httpx proxy for all /v1/* routes (streaming SSE pass-through + non-streaming). Config via LOOM_BIND (default 127.0.0.1), LOOM_PORT (8080), LOOM_VMLX_URL (http://127.0.0.1:8000), LOOM_LOG_LEVEL. 11 unit tests with respx-mocked backend, all passing. loom-gateway CLI entrypoint. session-verify updated to cover gateway."
    },
    {
      "id": "loom-003",
      "scope": "loom",
      "phase": 1,
      "title": "Gateway-side date/capability preamble for chat requests",
      "status": "done",
      "priority": "P1",
      "depends_on": [
        "loom-001"
      ],
      "spec": "When proxying POST /v1/chat/completions, the gateway injects a system-message preamble that (a) states today's date so the model never claims to be in its training-cutoff year and (b) tells the model it has no built-in internet/tools in this deployment and should ask the user for time-sensitive data instead of guessing. Existing system messages are augmented (preamble prepended), not replaced. Non-chat paths (GET /v1/models, etc.) are untouched. Behavior is opt-out via LOOM_INJECT_CONTEXT=false for transparent passthrough or for clients that already manage their own preamble. Pure stopgap for the gap that loom-004/005 will close properly.",
      "acceptance_criteria": [
        "POST /v1/chat/completions with no system message gets a system message prepended containing today's date in ISO format",
        "Existing system messages are preserved and appended after the preamble, not overwritten",
        "GET /v1/models and other non-chat paths pass through unmodified",
        "LOOM_INJECT_CONTEXT=false disables injection (transparent passthrough)",
        "Malformed JSON bodies pass through unchanged (never break the proxy)",
        "Unit tests cover all branches; live test confirms model answers current year correctly"
      ],
      "created_at": "2026-05-15T15:45:00Z",
      "updated_at": "2026-05-15T15:45:00Z",
      "notes": "Shipped 2026-05-15. Touches loom/gateway/src/loom/gateway/main.py (_inject_chat_context, _build_context_preamble), config.py (inject_context env reader), tests/test_gateway_unit.py (+17 tests). 28/28 unit tests pass; live curl to Qwen-3.6 35B mxfp8 returns '2026' for 'what year is it'. Date is recomputed per-request via date.today() so the model stays correct across day boundaries."
    },
    {
      "id": "loom-004",
      "scope": "loom",
      "phase": 1,
      "title": "MCP host plumbing in gateway (no tools yet)",
      "status": "todo",
      "priority": "P0",
      "depends_on": [
        "loom-003"
      ],
      "spec": "Add a Model Context Protocol (MCP) host to loom-gateway so future tool capabilities (web search, URL fetch, customer APIs, filesystem, etc.) all plug in through a single uniform protocol instead of becoming ad-hoc gateway code. Scope: connect to one or more MCP servers configured via LOOM_MCP_SERVERS (stdio or SSE transport), discover their tool catalogs at startup, expose the merged catalog to the model via OpenAI-style `tools` parameter on /v1/chat/completions, execute tool calls returned by the model against the right MCP server, and feed results back into the conversation until the model emits a final assistant message. Streaming SSE must continue to work end-to-end (tool calls and tool results stream as chat-completion chunks). Out of scope: shipping any specific tools (that's loom-005); auth on tool calls; per-conversation tool allowlisting (later).",
      "acceptance_criteria": [
        "Gateway can connect to N configured MCP servers at startup and aggregate their tool catalogs",
        "POST /v1/chat/completions with model that supports tool_use returns a response when the model decides not to call a tool (no behavior change vs today)",
        "When the model emits a tool_call, the gateway executes it via the right MCP server and continues the loop until a final assistant message",
        "SSE streaming works through the tool-call loop (chunks for assistant text, tool_call, tool_result)",
        "LOOM_MCP_SERVERS empty/unset \u2192 zero tools advertised, gateway behaves as a pure proxy (back-compat)",
        "Unit tests with a fake stdio MCP server (echoes a known tool); integration test with a real MCP server (e.g. mcp-server-time) confirming end-to-end loop"
      ],
      "created_at": "2026-05-15T15:45:00Z",
      "updated_at": "2026-05-15T15:45:00Z",
      "notes": null
    },
    {
      "id": "loom-005",
      "scope": "loom",
      "phase": 1,
      "title": "web_search and fetch_url tools as MCP servers",
      "status": "todo",
      "priority": "P0",
      "depends_on": [
        "loom-004"
      ],
      "spec": "Ship two first-party MCP servers under loom/mcp/ that the gateway can host via loom-004's plumbing: (1) web_search backed by a configurable provider (Brave/Tavily/DuckDuckGo HTML), returning top-N {title, url, snippet}; (2) fetch_url that retrieves a URL and returns text content (HTML \u2192 readable text via trafilatura or similar), with size cap and timeout. Both run as standalone processes; API keys come from macOS Keychain via the existing pattern, never env files. Out of scope: caching layer (later), per-tool rate limiting beyond a simple in-process cap.",
      "acceptance_criteria": [
        "loom/mcp/web_search/ and loom/mcp/fetch_url/ are installable Python packages exposing MCP servers",
        "web_search returns structured results from at least one configured provider (Brave default if BRAVE_API_KEY set, DuckDuckGo HTML fallback otherwise)",
        "fetch_url returns readable text with a 1 MB cap and 10 s default timeout; respects robots.txt for cooperative crawlers (opt-out via flag)",
        "End-to-end through gateway: chat asking 'what's the weather in Tokyo today' triggers a web_search call and the model produces a grounded answer",
        "Keychain integration for API keys (never plaintext); fallback message if no provider is configured",
        "Unit tests for each tool; integration test with gateway tool-loop"
      ],
      "created_at": "2026-05-15T15:45:00Z",
      "updated_at": "2026-05-15T15:45:00Z",
      "notes": "Closes the gap that loom-003 papers over. After this lands, LOOM_INJECT_CONTEXT can be revisited (preamble could mention 'you have web_search and fetch_url available' instead of 'you have nothing')."
    },
    {
      "id": "loom-006",
      "scope": "loom",
      "phase": 1,
      "title": "Visual polish pass \u2014 Claude-like typography, spacing, message layout",
      "status": "todo",
      "priority": "P1",
      "depends_on": [
        "loom-002"
      ],
      "spec": "Bring the web UI's look and feel close to Claude.ai using open-source equivalents (no Anthropic-licensed fonts). Switch UI typography to Inter (or system-ui) and message-body typography to a humanist serif (e.g. Source Serif 4, Crimson Pro) loaded locally from /fonts. Adopt an 8px spacing rhythm. Constrain the message column to ~720px max-width with generous side padding. Remove bubble around assistant messages (flat with subtle dividers); keep a soft surface for user messages right-aligned. Add hover/focus states to sidebar items, a loading shimmer on the currently-streaming assistant message, a refined empty state with 3\u20134 suggested prompt chips, and a collapsible sidebar that auto-hides under 768 px. No new features \u2014 visual only.",
      "acceptance_criteria": [
        "Fonts ship in loom/web/public/fonts and are referenced via @font-face (no Google Fonts CDN, no Anthropic-licensed fonts)",
        "Message column max-width \u2264 760 px with consistent left/right padding at all viewport sizes",
        "Assistant messages render without a surrounding bubble; user messages have a subtle surface",
        "Sidebar collapses below 768 px viewport with a hamburger toggle",
        "Loading shimmer visible on the streaming assistant message until first token arrives",
        "tsc, eslint, vitest all green; no regressions to existing 24 web tests"
      ],
      "created_at": "2026-05-15T17:50:00Z",
      "updated_at": "2026-05-15T17:50:00Z",
      "notes": null
    },
    {
      "id": "loom-007",
      "scope": "loom",
      "phase": 1,
      "title": "Artifacts pane \u2014 right-side preview for code, HTML, SVG, Markdown",
      "status": "todo",
      "priority": "P1",
      "depends_on": [
        "loom-006"
      ],
      "spec": "Add a right-side preview pane that activates when the model emits an artifact (a code block tagged with a sentinel, e.g. ```html artifact=\"page.html\"```, or via a tool call to a future `create_artifact` MCP tool). The pane splits the chat 50/50 with a draggable divider; collapses by default. Each artifact gets a chevron-paginated history; latest auto-focused while streaming. Renderers: HTML in sandboxed iframe (no network, no top-frame access), SVG inline, Markdown via existing renderer, code with syntax highlighting + copy. No execution of arbitrary JS in the chat tab (sandbox attribute only). Out of scope: editing artifacts inline, running Python (Pyodide), file-typed artifacts.",
      "acceptance_criteria": [
        "An assistant message containing a fenced block with ```html artifact=\"name.html\"``` opens the artifacts pane and renders sandboxed",
        "Pane is closable, resizable, and remembers width per session",
        "Multiple artifacts in one conversation are navigable via prev/next chevrons",
        "iframe carries sandbox=\"allow-same-origin\" only; no allow-scripts in the default sandbox without explicit user opt-in",
        "Vitest unit tests for the artifact-parser and the pane reducer; manual UI smoke covers HTML/SVG/Markdown",
        "Streaming partial artifacts render progressively (no flicker between tokens)"
      ],
      "created_at": "2026-05-15T17:50:00Z",
      "updated_at": "2026-05-15T17:50:00Z",
      "notes": null
    },
    {
      "id": "loom-008",
      "scope": "loom",
      "phase": 1,
      "title": "File uploads in composer (PDF, image, txt/md/csv)",
      "status": "blocked",
      "priority": "P1",
      "depends_on": [
        "loom-002"
      ],
      "spec": "Add drag-and-drop and click-to-attach in the composer. Supported types: PDF (extract text via pdfjs-dist), images (PNG/JPEG/WebP, base64-embedded as multimodal user-message parts), plain text/markdown/CSV (inlined as text). Attachments render as removable chips above the textarea. The web client sends OpenAI-compatible multipart messages (content as array of text+image_url parts). PDF text extraction runs client-side; size cap 10 MB per file, 5 files per turn. Gateway proxies the multimodal payload unchanged to vMLX. Out of scope: server-side OCR, audio/video, file-typed artifacts.",
      "acceptance_criteria": [
        "Drop or attach a PDF \u2192 chip appears; sending includes extracted text in the user message",
        "Drop or attach an image \u2192 chip appears with thumbnail; sending uses content[].image_url with a base64 data URL",
        "Plain text/markdown/CSV inlined verbatim under a header comment",
        "Size and count caps enforced with a toast on violation",
        "Vitest tests for the parser/formatter; manual smoke through gateway"
      ],
      "created_at": "2026-05-15T17:50:00Z",
      "updated_at": "2026-05-15T17:50:00Z",
      "notes": "BLOCKED on vMLX supporting a multimodal model (image content parts). Filed as P1 so vmlx side can be planned in parallel. Text-only attachment subset (PDF/txt/md/csv) could ship first if we split the task."
    }
  ]
}
